# Tiny native evaluation-journal qualification

Prepared package: `/Users/ashujo/.cache/dynamol-research/qm-journal-native-qualification-v1/`. The package manifest pins the exact reviewed candidate/helper, original worker, unchanged reviewed process supervisor, runtime inventory, requests, harness, failure injector, and 19 no-QM test evidence.

Two three-atom water profiles use DF RHF/STO-3G and DF RKS/B3LYPG/STO-3G/grid2, both with explicit def2-universal-jkfit auxiliary basis. Oxygen is fixed; both hydrogens can relax. Original and candidate receive exactly the same request bytes, including SCF and optimizer thresholds. Each profile must optimize in at most ten configured steps, exercise at least two coordinate geometries, and produce no more than twelve journal evaluations.

Paired original/candidate final energies must agree within 1e-10 Hartree, Cartesian gradients within 1e-8 Hartree/Bohr per component, and coordinates within 1e-8 Bohr per component. Callback cycle counts, SCF convergence, energy (1e-10 Hartree), and gradient norm (1e-8 Hartree/Bohr) must agree. Journal manifests, identity/state/method/source hashes, immutable unaccepted status, callback references, and every full gradient are checked. Every journal entry is independently recomputed by the original worker at its exact recorded Bohr coordinates; energy and gradient thresholds are 1e-8 Hartree and 1e-7 Hartree/Bohr per component. No record is assumed to be an accepted optimizer step.

A separate native RHF run injects ENOSPC at `os.link` for the first evaluation record, after the genuine gradient has completed. It must fail with no accepted arrays, no completed record, and no callback progress reference; the temporary write and exact failure are retained. Candidate/helper source bytes and all numerical kernels stay unchanged.

Resource controls: one native child at a time, one thread, 256 MB native allocation, sampled 1 GB RSS stop, 20 s native worker alarm and 22 s child deadline, at most 29 children, total native campaign target/bound 120 s. The process-group supervisor supplies reviewed cancellation/cleanup; the shared QM lock is held for the campaign. Disk admission requires35GiB free, with8GiB running reserve. Only root-authorized existing worker37089/controller37084 is excepted from exclusive admission, bound by PID, command, parent and unchanged input/source hashes; no signal or mutation is sent to it.

These tests qualify small numerical and persistence behavior only. They do not qualify full-parent gradients, large memory use, metal chemistry, a force field, checkpoint reuse, Hessian or molecular accuracy. Native launch was separately authorized by root after the other small native slot completed. All success and failure artifacts remain in the fresh `run` directory.
