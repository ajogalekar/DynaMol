# Independent review: no-write recovery evidence core

No known blocker remains for the scoped no-write core after the complete-object binding and array-header fixes. Final source SHA256: `e160aee512211df052261b1ef11b4277e81fd192cf71369dbd5c5f466674e187`; final tests: `436c705ea279b2d0925eb1cfb1e226ddc01f96c6903d45ce193a6c772406881d`. Adjacent materializer remains unchanged at `698c6d6727e365860e3d00debbf060c216425e31f6f7e1a6ac7d55eb0237fab2`; admitted materializer plan remains `8753021db5fe96d71fffcb29e7198b4ecf7b8668343389c9f110b81d76e43a20`.

This verdict covers read-only parent revalidation, complete materialized-request binding, and bounded finite-real-array parsing for internally generated, hash-bound worker evidence. It does not admit a launch, validate an actual accepted parent endpoint, qualify the separate conventional-ESP/RESP reader, accept charges or force fields, or establish a general hostile-archive security boundary. No native QM or fitting was performed. No active runtime, worker, backend, or central checkpoint was modified.

## Independent evidence

- All 18 final author software tests independently passed in 0.195 seconds. Their positive result fixtures use two invented atoms, explicitly marked synthetic. Earlier 15-test source and results remain preserved.
- All 68 extracted parent-admission statements are AST-identical to the reviewed, pinned materializer block before its first publication. Only a new function signature and a return of existing validated locals are added. The original numerical, method, atom/state, runtime/source, constraint, geometry and before/after artifact checks are retained.
- The final `revalidate_actual_parent` was called against the actual admitted plan and still-running parent. It raised `Accepted terminal parent absent; no request emitted` before native artifact reads or downstream runtime admission. No result/request/parent files were created by this read-only call. The observer and proof are retained in `v3-readonly-ast-check.json`.
- The original core accepted contradictory downstream-contract and parent preparation/source metadata in a temporary two-atom fixture. That reproduction remains preserved. The revised core evaluates the exact pinned materializer publication expressions and compares canonical JSON digests for all four payloads and the complete successful terminal report. Thus all five files, including previously unchecked policy/provider/fit/source/preparation fields, extra keys, Boolean-versus-number distinctions, and status/remaining-work claims are bound. A changed request with a recomputed local input digest still fails its independently derived expected-object comparison.
- The reader requires the exact five-file inventory and successful materializer status, checks all execution/acceptance flags false, binds script/plan/input/parent, and compares a second complete byte-hash snapshot before returning the first captured bytes and hashes. Partial publication, false/refused terminal results, symlink artifacts, extra files, changed geometry and contradictory complete payloads are covered by the tests. The filesystem snapshots are not a multi-file transaction; the required final successful report and complete inventory address interrupted materializer publication in this scoped workflow.
- The narrow v2-to-v3 source change affects only `real_arrays`; every other function AST is unchanged. NPY member names must match the expected unique inventory. Each header is parsed before `np.load`: supported header version, exact expected integer shape, real native dtype of at most eight bytes, and exact header-plus-payload length are checked. Total uncompressed member bytes and each declared numeric payload are capped at 64 MiB. Final loaded arrays must have exact shape and finite real contents. This is a payload cap, not a claim that process RSS equals 64 MiB.
- An independent 236-byte NPZ regression declares a large shape in its 128-byte header but contains no data. The corrected v3 rejects it at header-shape validation with zero calls to the intercepted NumPy array allocator. Duplicate, truncated and trailing-payload fixtures also reject before array loading. No large tensor or chemistry calculation is needed to exercise the final regression.

## Preserved review failures

The initial version of the allocation probe guarded `numpy.empty`, but the installed NumPy reader uses `numpy.ndarray`. That initial attempt reached immediate EOF after the declared virtual allocation in 0.67 seconds; no array data were read or filled, and peak RSS was not sampled. Its source and failure record are preserved rather than represented as a successful no-allocation test. The corrected probe intercepts `numpy.ndarray` before allocation; a subsequent report-only NumPy-integer serialization error is also preserved. The final v2 proof intercepts the allocation request, and the v3 regression shows the request is never made.

## Remaining forward handoff

The complete positive path still awaits a genuine terminal, converged recovery endpoint. Callers must obtain the context by invoking the pinned `revalidate_actual_parent` API; a caller-created dictionary is not an admission certificate. Synthetic contexts in the tests demonstrate serialization behavior only. The source-qualified core neither spawns processes nor fits charges. Native conventional-ESP binding, RESP execution/output/charge mapping, launcher lifecycle, and physical force-field validation are separate reviews and remain outside this verdict.

Evidence directory: `/Users/ashujo/.cache/dynamol-research/6oim-cold-evidence-independent-review-v1/`. The final artifact manifest preserves preliminary and final sources, reproducers, failures, tests, exact-AST checks, actual-parent refusal, and this verdict. Mutable Python caches and the manifest itself are excluded.
