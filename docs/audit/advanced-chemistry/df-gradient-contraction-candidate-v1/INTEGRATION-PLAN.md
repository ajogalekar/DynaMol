# Future integration plan — no active changes

1. Independently review the candidate and the retained small-array evidence. Preserve its current source hash. It is scoped to the exact real float64 `pij,qji->pq` call in `pyscf/df/grad/rhf.py:229`; it is not a generic replacement for `lib.einsum`.
2. If an integration trial is authorized, create a separate frozen provider/runtime copy. Change only that contraction call to the reviewed helper, with explicit tile/workspace/output limits. Do not globally monkeypatch einsum, edit the active installation, omit auxiliary-basis response, assume occupied-index symmetry, or alter molecular inputs, scientific method, parameters, constraints or acceptance thresholds. Reject unsupported inputs explicitly rather than silently substituting another result.
3. In the isolated copy, compare complete density-fitted gradients against the unmodified source at a few small deterministic, nonsymmetric geometries, with auxiliary-basis response enabled. Check energy/gradient consistency and appropriate finite differences. The existing array tests do not establish full-gradient dispatch or physical-model validation.
4. Only after numerical review, run one bounded resource probe using immutable copies of the intended input and current method. Retain source/runtime/request hashes, complete evaluation evidence and process memory samples. Keep the existing8e9-byte RSS cap, process-group wall-time/cancellation control, thread limit and local scratch. Verify actual peak RSS and numerical results; explicit tile-byte arithmetic cannot replace that measurement.
5. Evaluate elapsed time and numerical parity before enabling a release path. The right operand is repacked for each left auxiliary tile; correctness is unchanged, but full-scale performance has not been measured. A successful resource probe still does not qualify a covalent or metal force field.

Current state: helper and eight small tests only; no runtime integration, full-size tensor, new molecular energy/gradient calculation, active-worker edit or model-acceptance claim.
