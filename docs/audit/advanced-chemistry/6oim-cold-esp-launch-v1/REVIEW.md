# Cold conventional-HF ESP launch wrapper

The separate wrapper is ready to inspect a real completed parent and bound
materialized request. The actual still-running parent was refused with only a
failed controller receipt, an explicit null child PID, and no native launch.
No sotorasib ESP calculation or new fitted parameters exist yet.

Final source SHA: `66127b114c0f795a892d0d0ad0b4ff4bbb1c8961f934279363a2d235d67f5980`.
The executable source copy is under
`~/.cache/dynamol-research/6oim-resp-recovery-v1/prepared/cold-esp-launch-v1`.
Evidence is under `~/.cache/dynamol-research/reviews/6oim-cold-esp-launch-v3`;
earlier failed/superseded evidence is retained in v1 and v2.

The wrapper uses the exact reviewed generic supervisor, including signal
latching, deadlines and process-group cleanup. It independently verifies the
parent through the reviewed no-write core and binds all five materialized
objects. Request, worker, launcher, spec, native outputs and runtime files are
captured and checked again after execution. The 9,051 runtime payloads and six
links passed an actual read-only inventory check in 1.91 seconds. Extra
executable payloads reject. The documented evidence and bytecode exclusions
remain; the child uses isolated mode, disables bytecode writing and uses a fresh
bytecode-cache prefix so excluded cached code is not reused. No active runtime
was changed.

Limits remain two threads, a 2,000 MB native allocation hint, separately sampled
8 GB process and process-group bounds, four hours native time plus two minutes
for the outer timeout, and 35/8 GiB entry/running disk reserves. Sampling is not
an OS memory cap. Cleanup failures retain the owned PID or uncertainty, and an
observed memory-bound crossing is preserved even if the normal sample callback
did not execute. Success means numerical execution pending the separate ESP
reader; physical acceptance and simulation readiness stay false.

Seventeen focused software checks pass, including two tiny real child processes
for isolated interpreter flags/fresh caches and timeout cleanup. These are not
QM calculations or full-parent positive-path qualification. The first test run
used macOS's `/var` temporary alias against its `/private/var` resolved path;
only the fixture was corrected, with the failure retained. Independent review
found and confirmed fixes for extra-runtime-file admission and failed-cleanup
reporting. The final source binds the reviewed core and enables the launch
gate; all eleven functions remain AST-identical to the tested source.

Independent pre-enablement wrapper review:
`~/.cache/dynamol-research/reviews/6oim-cold-esp-launch-independent-final-v1/review.json`,
SHA `6e7f4c72b8c1d3fe285133675d223897beb3e8d691c908e4ff2cd13588a8f2f5`.
The core has its separate review at SHA
`7089a43da91bd59b05f500807327ef5811cdabf088d812907009749e3c80b49d`.
The native ESP science reader, actual charge-fitting executor, real converged
ESP, native charge handoff and full-complex validation remain separate work.
