# 1MNC cycle-8 recovery endpoint admission

The separate `analyze_1mnc_recovery_endpoint_v1.py` wrapper is ready for the queued 2000 MB recovery. Readiness establishes the exact restart lineage; no terminal endpoint existed when it was checked, and no native endpoint, checkpoint, or live gradient was read. No QM was launched.

The original `analyze_1mnc_anchored_endpoint.py` and its pinned input, policy, source atom IDs, and chemistry functions remain unchanged. The wrapper verifies that original analyzer's SHA before executing its captured source bytes. It delegates source loading and every chemistry/geometry diagnostic directly to those original functions. The original crystal coordinates remain `ctx['request']`; the full PLH, five Zn coordination distances, ten angles, 96 covalent bonds, 50 hydrogen parents, stereochemistry, cap reactions, 94-free-atom gradients, source-frame displacement limits, and contacts against 1,212 omitted source protein heavy atoms retain their original definitions and thresholds.

The only permitted request differences are the recovered completed cycle-8 coordinates, exact restoration of the three original cap-carbon coordinates (largest correction 5.775e-11 Å), removal of the original checkpoint for cold SCF, and native memory allocation 8000 to 2000 MB. The intermediate reviewed 5000 MB input must differ from this input solely in that allocation value. Charge, spin, atom identities/order, whole ligand, optimization settings, functional, basis, auxiliary basis, grid and SCF thresholds must remain exact. A pinned runtime relocation manifest, 9,051-file inventory bound into the controller specification, and the retained small-water implementation regression support the new runtime admission. That small regression does not establish accuracy for the full metal complex.

The copied numerical endpoint reader differs from its original only in the reviewed input hash, runtime hash, and required 2000 MB metadata. A source-equivalence check records these substitutions. All original method resolution, convergence, units, dimensions, finite-value, array membership, checksum, state and three-carbon constraint gates remain intact. Additional gates require the exact completed controller record, specification and source hashes; no live child; resource metadata; identical captured input/worker; no extra native files, checkpoint reuse or symlink artifacts; identical optimization and resolved-input records; and unchanged native evidence during reading. A failed controller cannot promote a native numerical result.

The wrapper currently emits **no downstream requests**. After every original chemistry and numerical endpoint gate passes, the next step remains a separately reviewed conventional gradient at the exact endpoint, with the original DF/conventional gradient thresholds. Passing this stage would still not establish a full unconstrained minimum, validate a Hessian, produce an accepted force field, or resolve the other structural zinc/calcium sites.

Run the wrapper with a fresh output path; it reads only the one pinned queued recovery:

```sh
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python docs/audit/advanced-chemistry/analyze_1mnc_recovery_endpoint_v1.py --output /Users/ashujo/.cache/dynamol-research/reviews/1mnc-recovery-endpoint-v1/terminal-review-v1
```

Verification: 53 new tests passed in 0.22 s, including genuine lineage, original-frame cycle-8 diagnostics, 29 unreviewed lineage/state changes, six artifact rejections, fourteen terminal-controller rejections, unchanged native-reader gates, and a no-terminal/no-native-read check. No synthetic accepted endpoint was written. The original 37-test evidence remains retained; the attempted repeat reached a FileProvider-evicted fixture and was stopped without changing it. Full evidence is in `/Users/ashujo/.cache/dynamol-research/reviews/1mnc-recovery-endpoint-v1/`.

Independent review follow-up: the original 53-test source and evidence remain intact in the first audit directory. The new wrapper now requires the `child_pid` key with explicit null, exact integer schema/resource fields, and explicit false model/simulation flags. A separate real-array dtype guard rejects complex values before chemistry delegation; the copied numerical reader itself remains unchanged. All **75** tests passed in 0.34 s after these corrections, including 11 metadata regressions and complex-dtype checks for all five array keys (even with zero imaginary components). Fresh readiness and source hashes are retained in `/Users/ashujo/.cache/dynamol-research/reviews/1mnc-recovery-endpoint-v2/`. No endpoint or chemical model was accepted.
