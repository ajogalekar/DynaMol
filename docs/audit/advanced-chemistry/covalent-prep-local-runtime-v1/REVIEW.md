# Local preparation runtime qualification

**Qualified for the tested preparation operations; root review is required before materializer admission.** No quantum calculation, optimization, charge fitting, actual accepted endpoint or force-field acceptance was performed or created.

The stable interpreter is `/Users/ashujo/.cache/dynamol-runtimes/covalent-prep-local-v1/bin/python`, with that exact directory required as `sys.prefix`. Python is 3.12.13; package versions are NumPy 2.4.6, RDKit 2025.9.6, ParmEd 4.3.1 and Pillow 12.3.0.

The admission artifact is `/Users/ashujo/.cache/dynamol-research/covalent-prep-local-runtime-v1/qualification.json`, SHA-256 **`a4bf3f2f4e296f7333596673c23deae7755733460c95489c2ccbce0d1368617e`**. It binds the inventory, imported files/libraries, chemistry checks, original geometry-check source and canonical cap hashes. Its status is `qualified_for_scoped_preparation`; it does not satisfy the materializer's independent actual-parent or RESP-provider gates.

## Package and path evidence

The inventory, `/Users/ashujo/.cache/dynamol-research/covalent-prep-local-runtime-v1/runtime-files.json`, SHA-256 **`2948f0700c2a0f308aaecdf26565ff5fc45e28513ec82d2178dbd856e34faeba`**, pins **2,180 regular files, three interpreter links and 224 native payload files**, totaling 173,660,795 bytes. All inventoried files were read back successfully, with no dataless flags. Generated bytecode, the runtime lock and qualification records are excluded. The shared external uv interpreter resolves outside Documents and has SHA-256 `f1c4ad59c4adf1e5dfb5e9f558f7b95e4d7a62387d4bb4572fcf84f42648fee3`.

All 2,162 hash-bearing RECORD entries across the four local packages match actual local bytes and lengths. The four unhashed RECORD self-entries are covered by the full inventory. Original package metadata was read only from the three specific distribution directories in `DynaMol/.venv`; no recursive Documents scan or original scientific-payload hydration was performed.

| Original package | Matching scientific payload declarations | METADATA/WHEEL | Differing RECORD entries |
| --- | ---: | --- | --- |
| NumPy 2.4.6 | 890 | Byte-identical | `bin/f2py`, `bin/numpy-config` launchers |
| RDKit 2025.9.6 | 965 | Byte-identical | None |
| ParmEd 4.3.1 | 129 | Byte-identical | `bin/parmed` launcher |

The original and new RECORD path sets are identical. The three differences are confined to entrypoint-wrapper records; original wrapper contents were not read, so their precise textual differences are not claimed. Original RECORD declarations match byte-verified local scientific payloads; this does **not** assert that every old installed payload was reread or matched its own RECORD after installation. Pillow's new payload was verified, but no old Pillow comparison was requested.

Seven original metadata reads initially hit the eight-second bound. Targeted Foundation hydration requests were made for exactly those seven paths; all subsequent reads succeeded. Both failed-read evidence and successful copies are preserved. No conclusion was inferred from an empty/timed-out read.

Actual isolated imports verified **223 package module files and 92 loaded user native images**. All package modules load from the new prefix, and every loaded native image and `sys.path` entry is outside Documents. The environment is a local virtual environment using a shared external uv interpreter, not a independently portable application bundle or complete standard-library distribution.

## Executed checks

The scientific/parsing checks completed in 2.173 seconds:

- RDKit parsed and round-tripped neutral, charged, zwitterionic and disconnected-ion examples; formal charges and fragment counts matched. An enantiomer pair retained distinct R/S assignments, benzene retained aromaticity, and an explicit invalid-valence example was rejected.
- The three canonical cap files matched the root-provided hashes exactly. The parsed cap has 94 atoms, 52 heavy atoms, one connected component and net formal charge zero. The SG–MOV_C25 single bond remains present, expected product hydrogen counts match, and the declared stereocenters remain CA=R and MOV_C20=S.
- The constraints' 18 fixed-charge entries match their atom indices/names, and their 12 cap indices sum to `-5.551115123125783e-17 e`, numerically neutral. This checks the pinned constraints' internal identity/charge consistency; their source-library transferability and physical suitability remain separate.
- The exact `check_geometry` function from source SHA-256 `e3810f144cb85035f391262bd6a0520f2450b235828dc2d171a9ddead36058c3` was extracted by AST and executed on canonical source geometry. Its positive path used **explicitly synthetic, in-memory accepted/converged metadata**. No scientific accepted-result file was written. Pending status, reversed atom identity, changed charge, moved frozen caps, inconsistent units and mirrored stereochemistry all rejected.
- ParmEd parsed a small synthetic MOL2 while preserving names, atomic numbers, coordinates, arbitrary parser-test partial charges and bonds. Its Amber restart parser preserved the matching three-atom coordinates. Pillow completed a tiny RGB PNG round-trip.

These checks establish the scoped local code paths and pinned input interpretation. They do not demonstrate arbitrary-complex preparation, a completed quantum endpoint, RESP executable compatibility, fitted charges, chemical-model correctness or force-field transferability.

`qualification.json` contains concrete admission requirements and exact evidence hashes. `checks.json`, `record-comparison.json`, `import-observations.json` and `runtime-files.json` remain in the local evidence directory; `evidence-pointer.json` identifies its final artifact manifest. No original backend or active runtime files were edited.
