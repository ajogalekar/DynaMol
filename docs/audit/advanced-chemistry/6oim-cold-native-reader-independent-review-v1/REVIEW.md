# Independent review: cold ESP and native charge-evidence reader

No remaining blocking finding for the stated bounded reader/parser scope after the producer-format correction. Final reader SHA256: `955fe3cdba694142464c738c014ee9f5b66f29303908e449127df373e54e5325`; final tests: `5b0bd7fae0f0cf0abbf34fd8b1aa5bd2607b2e13e7f8af02c8d419be8cdf7793`. Core `e160aee512211df052261b1ef11b4277e81fd192cf71369dbd5c5f466674e187` and materializer `698c6d6727e365860e3d00debbf060c216425e31f6f7e1a6ac7d55eb0237fab2` are unchanged. All eleven final author artifacts match manifest `e9ac65926cecbf1c29c74c4e98a7134ef8135441a26b7815164e10fe1e175c88`.

This verdict covers the actual-parent-gated completed-ESP reader and the lower-level native RESP output/charge parsers. It does not admit a RESP execution, qualify a complete native charge handoff, accept a force field, or validate any physical model. No new quantum, ESP, RESP or MD calculation ran during the review. Tests parse small software fixtures and previously retained synthetic RESP outputs only.

## Concrete correction and independent checks

The original reader would reject a genuine successful QM result: its complete resources dictionary omitted `memory_limit_kind`, which the pinned worker always emits as `PySCF allocation setting, not an operating-system hard limit`. The original synthetic fixture omitted the same field, masking the mismatch. The final change adds precisely this expected field; removal of that single dictionary entry restores the complete original module AST. No numerical method, geometry, charge or tolerance logic changed.

All 25 final software tests independently passed in 0.409 seconds. Independently reproduced the original rejection with a clearly marked two-atom software fixture carrying the true producer metadata, then verified that the same fixture passes the corrected reader. Two exact resource-assignment/update AST statements from the pinned worker were executed with scalar mock data; no native function or calculation was invoked. The new producer-derived regression compares their complete output with the fixture and rejects omission of the field. The preserved v1 tests, v2 tests and source delta make the failure and correction explicit.

The worker's other checked output fields were compared with the frozen producer source: completion phase/progress, SCF state/count/tolerances, arrays inventory and metadata, optimization-disabled record, expanded method metadata, nuclear/AO/electron inventory, thread reports, units, and independent ESP integral crosscheck. The original native RESP writer's column widths and precision are retained; the roundtrip tolerances match the original handoff. No new potential model, charge renormalization or relaxed numerical gate was introduced.

## Evidence associations

The public `read_completed_esp` entry revalidates the actual pinned parent before any downstream read. It then binds the separately reviewed/enabled launcher, all five complete materialized artifacts, the exact captured launcher/worker/input/specification, terminal controller receipt, and exact nine-file native output set. The final receipt must be cleanly terminal with successful exit, no child, consistent progress, matching source/input/output hashes, coherent times and the reviewed resource limits. All parent/materialized/launch evidence is revalidated at the end. Launcher lifecycle correctness remains inherited from its separate pinned review; this review does not rerun process tests.

The native ESP input must match the captured materialized bytes. Resolved input must equal the pinned worker's validated request. The result remains conventional all-electron RHF/6-31G* at the exact parent coordinates and generated grid, with no checkpoint initialization. The expanded parent basis/state is retained while the density-fitting auxiliary fields are removed and `density_fit` is false. Nuclear arrays, electron/AO count, Bohr/angstrom conversion and finite ESP arrays must agree; the provider's independent integral spot checks and their selected indices/tolerance are checked. The output checkpoint is hashed as output evidence only and is neither reused nor certified for density/geometry consistency.

The actual parent transitioned to a failed terminal state during this review. A pending-parent-specific precondition stopped rather than silently treating that change as a passing observation. The final public-reader check was then run against the unchanged failed terminal controller, SHA256 `ab835bd6145b258ea2ca359be2c785ec8a829e451dc412133f4722cb678d3c3e`. It refused with `Parent is running, failed or nonterminal` before launcher admission or job reads; no job was created. The terminal controller and the stopped earlier precondition are preserved. There is no genuine accepted parent or successful cold-ESP endpoint in this evidence.

## Charge transport scope

The RESP parser requires the complete output set, finite charge vectors and fit diagnostics, a complete punch table with nuclear order/IVARY and matching before/after charges, and the reported hyperbolic-restraint controls. It rejects a qout-only result, native open/stop errors, incomplete tables/diagnostics, nonfinite values and altered identity/control fields. Diagnostic validation is deliberately limited to format/finiteness; it neither independently recalculates fit quality nor supplies a new fit-quality threshold.

The joint-charge candidate binds both stage input hashes, canonical initial charges, stage2's initial vector to stage1's actual qout digest, and the common native ESP text back to the admitted arrays. Source graph and canonical constraints are fully bound to the frozen source. The unchanged original `validate_charges` AST retains all 94 joint charges, the 18 fixed canonical charges, methyl equivalences and neutral 12-atom cap-removal policy. The returned 82 retained entries preserve their original atom IDs and exact corresponding stage2 values. Previously computed synthetic native fits test this mapping without representing them as results for an accepted biological endpoint.

The pure RESP functions intentionally return `accepted=false` and `execution_provenance_admitted=false`. They are not an execution-admission boundary by themselves: callers must pass parsed, immutable evidence from the future reviewed stable-cache executor and its stage receipts. No raw-qout-only actual workflow is provided. That executor, actual terminal stage receipts, successful full native handoff, subsequent static mechanics/torsion checks and physical model acceptance remain pending.

Evidence: `/Users/ashujo/.cache/dynamol-research/6oim-cold-native-reader-independent-review-v1/`. It preserves both sources/tests, producer mismatch evidence, exact delta, independent test outputs, actual failed-parent refusal, final author-manifest verification and this verdict. An initial literal-line subtraction check was replaced by an AST-exact dictionary-field comparison because the inserted field also moved a closing brace; this was a review-harness formatting issue, not a further source change.
