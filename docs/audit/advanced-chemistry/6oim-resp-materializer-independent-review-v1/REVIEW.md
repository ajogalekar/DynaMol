# Independent review: recovery ESP request materializer

No blocking finding remains for the scoped **no-launch request materializer and pure cold-ESP contract validator** after the narrow admission fixes. Reviewed source: `698c6d6727e365860e3d00debbf060c216425e31f6f7e1a6ac7d55eb0237fab2`; tests: `cac71b1fea9b29e62a3f4909504aa83af64362fed1a7d8e16b37c95795a1b914`.

This verdict does not admit a calculation launch, accept a parent endpoint, validate fitted charges, qualify a force field, or qualify the separate immutable cold-ESP reader/RESP handoff. No QM or fitting was run in this review. No active worker, runtime, backend, or central status was changed.

## Evidence checked

- Independently reran all 37 final software tests: passed in 0.013 seconds. The actual runtime verification functions rehashed the admitted preparation runtime and the separate RESP provider/evidence successfully in 0.459 seconds, without executing RESP. All 14 frozen chemistry source files also rehashed correctly.
- Reproduced three original pure-validator gaps using only the author's two-atom synthetic software fixture: a Boolean actual coordinate, a Boolean actual grid coordinate, and a floating-point SCF count were previously accepted through Python numeric equality. All three are rejected by the final source. Original source and reproductions remain preserved.
- Reviewed the added parent predicates against the frozen worker's actual output construction. Native output emits integer cycle counts, completed SCF state, the checked energy/coordinate/gradient unit labels, and the spherical/symmetry/functional/grid flags. New tests reject contradictory versions. No numerical method, tolerance, gradient, geometry algorithm, or charge constraint was changed.
- Independently called the final materializer against the current actual parent and the admitted plan. The absent terminal controller caused refusal before any native optimization artifact read. The new output folder contains only `result.json`, with no ESP request and every launch/acceptance flag false. A read-path observer and a forbidden downstream-admission stub establish the early refusal boundary.
- The actual plan path is `/Users/ashujo/.cache/dynamol-research/6oim-resp-recovery-v1/prepared/plan-v3-resp-admitted.json`, SHA256 `8753021db5fe96d71fffcb29e7198b4ecf7b8668343389c9f110b81d76e43a20`. This review binds that plan, not an absent top-level plan path.

## Admission and scientific continuity

The parent must have the exact terminal controller status, no child PID, exact controller/spec/input/worker/runtime pins, and the reviewed 2-thread, 2000 MB allocation, 8-billion-byte sampled RSS and 21720-second resource profile. The controller result binds the native result. Native inputs and all eleven native artifacts are checked, and all eleven hashes plus the controller are checked again before publication. A failed, pending or changed parent cannot reach publication through the reviewed path.

The native result must be accepted and complete with converged SCF and converged optimization under the exact original resolved settings. Atom IDs/order, neutral closed-shell state, 94-atom inventory, 388 electrons and 816 AOs are checked. The all-electron nuclei, finite arrays, expanded RHF/6-31G* basis, def2-universal-jkfit auxiliary basis, source hashes and emitted method metadata remain bound. Native constraint text and its digest must match the requested frozen atoms. The exact pinned original geometry checker is extracted through AST; its bond geometry, stereochemistry and frozen-cap checks remain unchanged.

The ESP request uses the admitted endpoint coordinates and regenerated four-shell grid, with explicit conventional RHF/6-31G* and `density_fit=False`. The parent optimization retains its density-fitted method. This is the already planned mixed workflow, not a claim that density fitting and conventional SCF are identical. Any `initial_checkpoint` key is prohibited in this branch. The stored checkpoint is hashed as parent provenance but is not reused or represented as geometry validated. The pure cold-ESP validator additionally checks convergence, state/order, exact geometry/grid, units, finite potential values, method signatures, and externally supplied file hashes. Its docstring correctly requires a separately reviewed file-binding reader.

All four original RESP inputs remain exactly pinned: constraints, both stage inputs and canonical initial charges. The 18 fixed source charges, full 94-atom joint fit, four methyl equivalence groups, 12 neutral cap atoms and retained-atom mapping remain inherited from the original source/assembly plan. This materializer performs no charge fit, cap removal or renormalization; native execution and mapping checks belong to the subsequent handoff.

## Remaining qualification limits

There is no genuine accepted recovery endpoint yet, so the complete positive native-parent-to-request path has not been exercised. Synthetic metadata tests are software evidence only. A future successful endpoint still requires actual geometry review, a converged cold conventional ESP, separately reviewed immutable-file binding, native RESP validation and the remaining force-field/torsion checks.

Publication is atomic and no-clobber **per file**, with file and directory fsync. It is not a transaction covering all output files. A late publication error can leave earlier artifacts, and a directory-fsync error after link creation can leave the linked file. Consumers must require the final successful materializer report, hash-bind every emitted artifact, and reject partial/refused output folders. The new reader/launcher review must enforce that boundary. No cancellation or child-process lifecycle is claimed by this no-launch module.

The verdict is specific to this pinned recovery and these frozen sources. It does not establish transferability to other adducts or validate the physical model for production MD.

## Retained artifacts

Local evidence: `/Users/ashujo/.cache/dynamol-research/6oim-resp-materializer-independent-review-v1/`. It preserves the original source/tests, initial reproductions, final source/tests/diffs, independent test output, `review_checks.py`, `review-checks.json`, and both actual-parent refusal observations. The artifact manifest excludes mutable Python caches and itself. The separate immutable reader and native-charge branch will receive their own verdict.
